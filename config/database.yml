# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
production:
  adapter: postgresql
  database: pinpoint_production
  username: pinpoint
  password: P1nP01nt!
  pool: 25
  host: localhost

