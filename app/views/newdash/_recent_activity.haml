.spaced.store-lookup
  %legend
    %i.icon-home
    Store look-up
  %form.form-horizontal{:action => lookup_stores_path}
    %fieldset
      %label{:for => "account_number"} Account Number
      %input#account_number.typeahead-account{:autocomplete => "off", "data-provide" => "typeahead", :name => "account_number", :type => "text"}/
      %input.btn.btn-primary{:type => "submit", :value => "Show"}/
.spaced.recent-activity
  %legend
    %i.icon-time
    Recent Activity
    %small= link_to "View more", activity_reports_path(type: :order), :class=> 'pull-right'
  %table.table.table-striped.table-hover.spaced-out
    %thead
      %tr
        %th Date
        %th User
        %th Event
        %th Type
    %tbody
      - my_recent_activity.each do |t|
        %tr
          %td= t.created_at.strftime('%d %b %Y %H:%M')
          %td= version_user_name t
          %td= t.event
          %td= version_type t
