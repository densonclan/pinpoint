- content_for :title do
  Exporter Templates
= breadcrumbs [['Exporter', exporter_index_path], ['Exporter Templates']]
.page-header
  %h1
    %span
      %i.icon-circle-arrow-up.icon-2x
    Exporter Templates
= render 'exporter/menu'
%table.table.table-striped.table-hover
  %thead
    %tr
      %th Name
      %th Last Updated
      %th Value
      %th Actions
  %tbody
    - @templates.sort! { |a,b| a.name.downcase <=> b.name.downcase }
    - @templates.each do |t|
      %tr
        %td= t.name
        %td= t.updated_at.strftime('%d %B %Y at %H:%M:%S')
        %td= t.value
        %td
          = link_to "Edit", edit_export_template_path(t)
          = link_to "Delete", t, confirm: 'Are you sure you want to delete this template?', method: 'delete'